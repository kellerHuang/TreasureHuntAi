#!/usr/bin/python3

import copy

# a bfs to a point calculating the minimum amount of resources to get to that point
# the map must be explored

origin = (2,2)
playerx = 3
playery = 2


allview =[]
def pathBack():
    player
    test = copy.deepcopy(allview)
    while change == 1:
        for i in range(sizex):
            for j in range(sizey):
                if test[j][i] == player: